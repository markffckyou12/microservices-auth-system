import { Pool } from 'pg';
import { RedisClientType } from 'redis';
export default function createSessionRoutes(db: Pool, redis: RedisClientType): import("express-serve-static-core").Router;
//# sourceMappingURL=session.d.ts.map