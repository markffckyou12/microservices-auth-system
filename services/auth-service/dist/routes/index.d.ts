import { Express } from 'express';
import { Pool } from 'pg';
import { RedisClientType } from 'redis';
export declare const setupRoutes: (app: Express, db: Pool, redis: RedisClientType) => void;
//# sourceMappingURL=index.d.ts.map